# TODO

## IR / spec issues (post IR-agent review)

### 1. Add `shape` to Table type; validate `len(indices) == len(shape)`

**Files:** `rust/crates/ir/src/table.rs`, `rust/crates/ir/src/validate.rs`,
`ocaml/lib/ir/ir.ml`, `ocaml/lib/ir/validate.ml`, all golden `.ir.json` files
**Issue:** Design change (approved): `TableLookup` takes a true multi-index
`indices: Vec<Expr>` (already implemented by IR agent) with row-major stride
semantics. But `Table` currently has no `shape` field — the runtime cannot know
strides or validate index count. Spec §3.4 updated to reflect this design.
**Fix:**

1. Add `shape: Vec<usize>` to `Table` struct in Rust and OCaml
2. ~~Update golden files~~ — done: `seir_age.ir.json` C_age gets
   `"shape": [2,2]`, lookups updated to `[i,j]` form; all other golden files
   have empty tables
3. Change validation from "warn on len > 1" to error on
   `len(indices) != len(shape)`
4. Runtime evaluator uses precomputed strides (see spec §3.4) **Note:** Runtime
   agent is currently running — do not change `crates/ir` until it lands. Fix in
   follow-up pass immediately after merge.

### 2. `Projected` Expr node — validate context

**File:** `rust/crates/ir/src/validate.rs`, `ocaml/lib/ir/validate.ml`
**Issue:** `Projected` is a new Expr variant added by the IR agent (not in spec)
for use inside likelihood expressions (so a likelihood can reference its own
projection output). The validator supposedly rejects `Projected` outside
likelihood contexts, but this has not been verified. If the runtime agent's
propensity evaluator encounters `Projected` in a transition rate, it will either
panic or silently return wrong results. **Fix:** Read validate.rs/validate.ml,
confirm `Projected` is rejected in transition rate / ODE derivative /
intervention value contexts. Add a test.

### 3. Write test_golden.ml — OCaml becomes source of truth for golden files

**File:** `ocaml/test/test_golden.ml` **Issue:** Current golden JSON files in
`ir/golden/` are hand-authored bootstrap artifacts. They should be generated by
OCaml serialization of embedded `Ir.model` values, with JSON committed as
derived artifacts. Regenerated via `make update-goldens`. **Fix:**

1. Write `test_golden.ml` with one `Ir.model` value per golden model
2. Tests: (a) serialize → write `ir/golden/*.ir.json`; (b) serialize →
   deserialize → assert structural equality
3. Add `update-goldens` target to Makefile
4. After this lands, hand-authored JSON is replaced by OCaml-generated JSON

### 4. Update spec §11 Expr JSON examples

**File:** `compartmental-ir-spec.md` **Issue:** §11 (the Cholera example) uses
an `op/args` style for Expr JSON that does not match what was actually
implemented (`{op, left, right}` / `{op, arg}` tagged-union style). Spec and
implementation are inconsistent. **Fix:** Update §11 JSON examples to match
implemented encoding. Done when runtime agent lands to avoid churn during active
development.
